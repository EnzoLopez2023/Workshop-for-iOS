# Retired App Store evidence

> [!IMPORTANT]
> **PRESERVATION ONLY since 2026-08-31.** Workshop for iOS was a
> TestFlight-only, single-user app retired because its sole owner/tester uses
> <https://workshop.nintek.com> exclusively and did not use the native-only
> features. Do not regenerate release assets, upload a build, restore beta
> access, prepare a reviewer account, create a submission, or release the app.

## Retirement record

- Final functional main source:
  `5be546524e79b9c63b2a4effb5ec24e03fe6d777`; final native version/build:
  `2.3.0 (15)`.
- All 16 uploaded builds from `0.1.0` through `2.3.0` are expired, with internal
  and external beta states `EXPIRED`.
- The sole internal beta group was emptied and deleted, two tester memberships
  were removed, and app-scoped unlink requests were accepted asynchronously.
  No external beta group or public link exists.
- App `6793709356` retains version record
  `ab8c64ab-bf76-4c14-9b63-ab58630a59db` for `2.2.1` in
  `PREPARE_FOR_SUBMISSION`, attached to expired build 13.
- A historical review submission is `COMPLETE` and its only item is `REMOVED`.
  The item was removed before public release. No current version submission
  exists, and public App Store lookup returns zero results.

## RETIRED - RESERVED

Team `3KB968X34U`, app `6793709356`, version record
`ab8c64ab-bf76-4c14-9b63-ab58630a59db`, and bundle IDs
`com.nintek.workshop`, `com.nintek.workshop.widgets`, and
`com.nintek.workshop.share` must never be deleted, transferred, recycled, or
repurposed. They retain Apple ownership, signing and entitlement continuity,
historical records, and compatibility with retained installations or dormant
accounts.

## Evidence handling

- `APP_STORE_STATUS.md` is the current retirement authority.
- Everything in this directory is preserved historical evidence. Imperative or
  future-facing language inside evidence payloads records abandoned release
  gates and is not authorization to execute them.
- Do not edit or regenerate `RELEASE_VISUAL_MANIFEST.json`,
  `APP_PRIVACY_EVIDENCE.md`, `P2-09_PROVIDER_SCOPED_ACCOUNTS.md`, or
  `GUIDELINE_2_1_TEMPLATE.txt` merely to restate retirement.
- Do not run `Scripts/capture-app-store-screenshots.sh` or replace local masters.
  The capture commands, ignored `Captures/` and `Screenshots/` directories, and
  their historical dimensions remain evidence only.
- Preserve the three app-icon renditions, screenshots, metadata, source, tests,
  assets, git history, tags, branches, Xcode project, Sign in with Apple
  capability, Apple keys, and signing evidence.
- Allow app-specific provisioning profiles to expire naturally. Never revoke
  shared certificates or keys.
- Preserve dormant Apple backend/account compatibility until a separate phase is
  approved. Do not delete or mutate accounts as part of this documentation
  retirement.

The prior title-art compositor and seven outputs remain recoverable from git
history at commit `5e1d064`. They are historical, not current submission assets.
Repository archival or deletion remains a separate owner action.

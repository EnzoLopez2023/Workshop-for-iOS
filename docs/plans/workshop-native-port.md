# Workshop → Native SwiftUI Port ("Workshop for iOS") — Full Phased Plan

> **COLD-START NOTE (for a fresh session with no prior context):** This plan is
> self-contained — all decisions below were made with Enzo on 2026-07-22 after a
> full exploration of the React app, its backend, and the completed ShopKeep port.
> Before writing any code, skim these three references:
> 1. `/Users/enzo/repos/Xcode - SwiftUI/NintekKit/docs/NATIVE_PORT_PLAYBOOK.md` — the port rules (esp. "the .tsx is the spec, not the screenshot").
> 2. `/Users/enzo/repos/Xcode - SwiftUI/ShopKeepNative/` — the completed sibling port; lift its patterns per the "Reusable assets" section below.
> 3. `/Users/enzo/repos/VSCode - React/workshop/` — the app being ported; `server.js` is the API contract.
> Work phase by phase, commit-and-push per feature, bump the version every commit,
> and pause for Enzo's device-testing loop at the end of each phase.

## Context

Port the Workshop React app (`/Users/enzo/repos/VSCode - React/workshop` — woodworking project journal: projects, cut lists, materials, cut-plan optimizer, Shaper CNC hub, shopping list, build/finish logs) to a native SwiftUI app at `/Users/enzo/repos/Xcode - SwiftUI/Workshop for iOS`. This is the next port in the portfolio strategy (Cairn → ShopKeep → **Workshop**), executed with every lesson from the ShopKeep port (23+ commits, v1.6.0, playbook at `NintekKit/docs/NATIVE_PORT_PLAYBOOK.md`). End state: near-identical native app matching the web's features and warm cream/rust editorial look, plus native additions (haptics, widgets, Sign in with Apple, App Intents). This plan lives at `Workshop for iOS/docs/plans/workshop-native-port.md` — keep it updated (check off phases, record decisions) as the port progresses.

**Locked decisions (Enzo):**
| Axis | Decision |
|---|---|
| Auth | **Both providers, like ShopKeep v2.4.0** — port ShopKeep's dual-auth (Entra + Apple→server-session) to workshop/server.js; native offers Microsoft (MSAL) + Sign in with Apple. Data keyed per provider. |
| Sync | Non-negotiable — keep Azure backend; native = SwiftUI client of existing API |
| Audience | Personal / TestFlight near-term |
| Targets | iPhone + iPad adaptive (ShopKeep RootView pattern) |
| Notebook (Tabloom) | **Skip for v1** (dead server tables; it's a separate Tabloom-token integration) |
| Widgets | In-progress projects + project stats |
| AI URL-import | Include both (projects + Shaper) — call existing analyze-url endpoints |
| Typography | **System font only** (SF app-wide, like ShopKeep native — no serif headers) |
| Versioning | project.yml single source of truth; **bump every commit** (semver marketing + monotonic build), start 0.1.0 → 1.0.0 at TestFlight |

## Key exploration facts

### The web app (the spec — "the .tsx is the spec, not the screenshot")
~7.9k LOC TS/TSX. Pages: Dashboard 427 · ProjectForm 928 · ProjectDetail 1054 · ShaperProjectForm 500 · ShaperProjectDetail 307 · ConversionTables 320 (pure client) · ShoppingList 199 · Settings 231 · Notebook (skipped). Components: CutPlanOptimizer 505 · CutPlanSheet 167 (SVG diagrams) · CommandPalette 213 · cards/skeletons/lightbox.
- **`src/lib/cutPlan.ts` (331 LOC, pure TS, zero deps)** — guillotine/BSSF packer (`optimizeCuts`, `guillotineSplit`, `bssfScore = leftoverShort*1e6+leftoverLong`) + **`parseInches`** (fractions, feet-inches, vulgar fractions, smart quotes — load-bearing, port carefully).
- Design tokens (`src/index.css`): cream `#F5F0EA`, paper `#FFF`, ink `#1C0F07`, muted `#8B7A6B`, rust `#A0522D`, line `#EDE8E3`; dark mode walnut/copper under `[data-theme=dark]`; 5 accent presets (rust/forest/slate/amber/navy); per-route photo backgrounds with veil. Same family as ShopKeep — reuse ShopKeep's `Theme`/`Palette` architecture with Workshop values.

### The backend (`workshop/server.js`, 1594 lines — keep, extend for Apple)
- Express 5 + better-sqlite3, **per-user DB** `USERS_DIR/<oid>.db`, jose JWKS validation. **Stricter than ShopKeep:** single issuer (`…/v2.0`) + single audience (`API_AUDIENCE`), Entra-only, `OID_RE` GUID-only user keys. Native MSAL tokens + Apple keys need ShopKeep's `ACCEPTED_ISSUERS`/`ACCEPTED_AUDIENCES`/`USER_KEY_RE` treatment.
- ~50 snake_case routes: projects CRUD (fat detail aggregate: images/cut_list/materials/build_log/finish_log/links), images (**multipart** upload, disk storage, 200MB, MIME-sniffed, PDFs allowed; GET auth-exempt via `?oid=`), cut-list, materials (+`PATCH …/purchased`), shopping-list, build-log (multipart photo), finish-log, links, templates (save-as/clone), shaper-projects (+images/cut-list), cut-plan-config (opaque JSON `{config:…}`), 2× AI analyze-url (Claude `claude-sonnet-4-6`, 30/h rate limit).
- Decoder gotchas: dimensions are **strings** ("27 1/2"); `purchased` bool; `wood_types`/`tools_needed` hydrated arrays; mixed create responses (full object vs `{id}` vs `{success:true}`); most fields nullable.
- Deploy: Azure App Service container `app-workshop-prod-lwxhu7jxlrbtu` (GH Actions → ACR on push to main), custom domain `workshop.enzolopez.net`, data at `/home/data`. **Entra client/tenant GUIDs are NOT in the repo** — pull from App Service config (`AZURE_TENANT_ID`, `API_AUDIENCE`, `VITE_AZURE_CLIENT_ID`) before auth wiring.

### Reusable ShopKeep-port assets (lift wholesale)
- `ShopKeepNative/ShopKeep/App/Theme.swift` + `Palette.swift` + `ThemeManager` — token architecture, `skCard()`, `creamBackground()`, `contentColumn()`, `AppInfo.version`, `Eyebrow`.
- `App/RootView.swift` — `AppDestination` enum, iPad `NavigationSplitView` (portrait-collapse/landscape-both, sidebar 216/260/272, brand header + signed-in/version footer, `.ignoresSafeArea(.keyboard)` fix), iPhone `TabView`.
- `Auth/MSALAuth.swift` (keychain-group gotcha), `Auth/AppleAuth.swift` + `AppleSessionStore` + `SessionTokenProvider` (refresh ≤60s of expiry), `SignInView` (both buttons, Apple name capture), `AppModel` provider-picking init, `onOpenURL` deep-link-then-MSAL.
- `App/AuthImage.swift` (actor cache) — adapt: Workshop images are `?oid=`-scoped GETs, so build URL with `?oid=<userKey>` (like web) rather than bearer-only.
- `TaxonomyEditor` pattern (config-of-closures CRUD), `FlowLayout`, sort-comparator lesson (swap operands, never negate), search `FocusState` fix, `Exporter`+`ShareSheet`, `CameraPicker`, `ScannerSheet` (if needed later), Widgets extension + `NSExtensionPointIdentifier` trap, deep links via `model.pendingSearch`.
- `NintekKit` — `APIClient` (get/post/put/delete/postRawJSON/deleteWithBody/getData), `TokenProvider` seam, `APIError`, snake_case decoder/encoder config from `ShopKeepAPI.swift` init. **Gap: no multipart support — must add `postMultipart` to APIClient** (Workshop uploads are FormData, not base64 JSON).

### Fresh Xcode template (replace)
Stock SwiftUI+SwiftData template, 1 commit, no remote. Wrong everything: plain Xcode (not xcodegen), deployment target **26.5**, Swift 5.0, bundle id `nintek.com.Workshop-for-iOS` (inverted). **Re-scaffold with xcodegen**: target name `Workshop` (space-free; folder stays "Workshop for iOS"), bundle id `com.nintek.workshop`, iOS 17.0, Swift 6.0, team `3KB968X34U`, version-in-settings pattern. Keep the git repo; add GitHub remote later.

---

## Phase 0 — Prerequisites (backend + config, before any Swift)

> **PROGRESS (2026-07-22): Phase 0 COMPLETE.** 0.3 ✅ re-scaffold (`d10f83d`, iOS repo).
> 0.2 ✅ dual-auth **merged to main + deployed to prod** (workshop repo merge `371184f`),
> verified live: `/api/auth/apple` → 401/400 (enabled, not 503), Entra no-token → 401,
> SPA + `/api/health` → 200. 0.1 ✅ Entra iOS redirect/scope added by Enzo; App Service
> env `SESSION_SECRET` + `APPLE_BUNDLE_ID=com.nintek.workshop` set. **Next: Phase 1.**
> (Deploy note: the GH Actions health-poll can pass against the old container mid
> rolling-swap — re-test prod ~2 min post-deploy before concluding anything.)

**0.1 Pull Entra config** (needs Enzo/az): ~~read `AZURE_TENANT_ID`, `API_AUDIENCE`, `VITE_AZURE_CLIENT_ID`~~ — **the GUIDs are already in the repo** at `.github/workflows/deploy.yml` (`VITE_AZURE_CLIENT_ID: 0f303f8f-207f-4b7f-84a5-b5d0abcf49d1`, `VITE_AZURE_TENANT_ID: 52188f12-db6b-46c6-88ff-08c802f0ed3b`; `API_AUDIENCE` == the client id). No `az` needed for IDs. **Still needs Enzo in the portal:** verify the Entra app registration has **Expose an API** scope (`api://<clientId>/access_as_user`) AND add an **iOS platform redirect** `msauth.com.nintek.workshop://auth` (the two ShopKeep deploy landmines: missing runtime env vars → 503; missing scope → AADSTS500011 login loop). **Plus the new App Service env for 0.2 (below): `SESSION_SECRET`, `APPLE_BUNDLE_ID=com.nintek.workshop` — set these BEFORE merging `feat/dual-auth-apple` to main.**

**0.2 Port dual-auth to `workshop/server.js`** ✅ **DONE (branch `feat/dual-auth-apple`, commit `34297a5`, verified locally, NOT pushed).** As built:
- `ACCEPTED_ISSUERS` (v2.0 + `sts.windows.net/<tenant>/`) + `ACCEPTED_AUDIENCES` (`api://<id>` + bare id) — strict superset of the old single-value check, so no web/Entra regression; fixes the native-token 401.
- Apple path: `APPLE_JWKS`/`APPLE_ISSUER`/`APPLE_AUDIENCES` verify, `POST /api/auth/apple` (id_token → minted HMAC session access+refresh, `apple_<sha256(sub)>` userKey), `POST /api/auth/refresh`, `verifySession`/`mintSession`, `userKeyFromBearer` (own session first, then Entra), `USER_KEY_RE` (GUID or `apple_<64hex>`) gating `getUserDb`+`resolveReadDb`, `upsertProfile`/`readProfile`/`user_profile` table. Gated by `SESSION_SECRET` (`APPLE_AUTH_ENABLED`) → deploys dark, 503s until configured, Entra path unaffected.
- **Empty-seed decision (Enzo, 2026-07-22): new non-primary users get a BLANK schema DB, not a copy of the primary user's data.** Dropped the `copyFileSync(SEED_DB_PATH, …)` in `getUserDb`; the seed snapshot now backs demo mode (`getDemoDb`) only. This closes the exposure that opening Apple sign-in would otherwise create (any Apple ID would have inherited Enzo's real projects). Session names: `SESSION_ISSUER='workshop-api'`, `SESSION_AUDIENCE='workshop-clients'`. Env used: `SESSION_SECRET`, `APPLE_BUNDLE_ID` (native audience), optional `APPLE_WEB_SERVICES_ID` (web, later).
- **Remaining (Enzo):** set App Service env (0.1b) → merge `feat/dual-auth-apple` → main to deploy → verify `/api/health` + a real native token round-trip (**test acceptance, not just rejection** — playbook rule) + web regression.

**0.3 Re-scaffold the Xcode project** ✅ **DONE (iOS repo, commit `d10f83d`).** xcodegen `project.yml` cloned from ShopKeepNative: target `Workshop`, bundle `com.nintek.workshop`, iOS 17, Swift 6, team 3KB968X34U, version 0.1.0/build 1, MSAL+NintekKit. Widgets deferred to Phase 6. `Workshop/App/WorkshopApp.swift` placeholder; builds clean for generic iOS. Dual-auth URL schemes + Apple/App-Group/keychain entitlements declared up front.

## Phase 1 — Foundation (NintekKit + auth + theme + shell)

**1.1 NintekKit additions** ✅ **DONE (NintekKit repo, commit `dd9b4e0`; 19 tests green; iOS app builds against it).** As built: `Models/WorkshopModels.swift` (WSProject with aggregates optional so bare create/update rows decode; WSProjectDetail fat aggregate; all the row/input/analyze types; `CutPlanConfig`/`StockRow` camelCase-coded for web↔native interop), `WorkshopAPI.swift` (~35 routes, snake_case coders, `imageURL(imageId:userKey:)`/`buildLogImageURL(entryId:userKey:)` with `?oid=`, cut-plan-config via plain coders), `APIClient` +`postMultipart`/`patch`/`putRawJSON`/`MultipartFile` (additive — ShopKeep untouched), plus 6 decode tests. `CutPlan.swift` still deferred to Phase 4. Original spec below:
- `Models/WorkshopModels.swift`: `WSProject` (list row + detail aggregate), `WSImage`, `CutListItem`, `WSMaterial`, `BuildLogEntry`, `FinishLogEntry`, `ProjectLink`, `WSTemplate`, `ShaperProject`, `ShoppingItem`, `AnalyzeResult`/`ShaperAnalyzeResult`, `CutPlanConfig` (Codable mirror of the web's stock/kerf config JSON). All optionals-heavy, snake_case-decoded; dimensions as `String`.
- `WorkshopAPI.swift` modeled on `ShopKeepAPI.swift`: one method per route (~35 methods), `productionBaseURL = https://app-workshop-prod-lwxhu7jxlrbtu.azurewebsites.net`, mixed-response handling per endpoint (`{id}` vs full vs `{success}`), `imageURL(_:userKey:)` appending `?oid=`.
- **`APIClient.postMultipart(path:fields:file:)`** — new: build `multipart/form-data` body (boundary, `file` field with filename+MIME, extra text fields) for image/build-log uploads.
- `CutPlan.swift` port lands here too (Phase 4) so it's unit-testable in the package.

**1.2 App target skeleton** ✅ **CODE DONE (iOS repo, commit `32bbd92`, build 5; builds device+simulator; launches & renders SignInView in the simulator).** As built: `App/` — `WorkshopApp.swift`, `AppModel.swift` (provider-picking init dev-token→Apple→MSAL, `userKey` accessor for `?oid=`, `WORKSHOP_API_BASE`/`WORKSHOP_DEV_TOKEN` overrides, `workshop://project/:id` deep links), `RootView.swift` (dashboard/projects/shaper/shopping/more; iPhone TabView + iPad NavigationSplitView), `Theme.swift`+`Palette.swift` (Workshop cream/rust/ink light+dark from index.css; 5 accent presets rust/forest/slate/amber/navy over shared surfaces; `wsCard`/`creamBackground`/`contentColumn`/`Eyebrow`; system font; `configureAppearance` @MainActor), `AuthImage.swift` (URL/`?oid=` actor cache), `PresentationAnchor.swift`. `Auth/` — MSALAuth (Workshop client/tenant/redirect), AppleAuth+SessionStore+SessionTokenProvider, SignInView (both buttons, no demo mode). Placeholder destination screens; Dashboard does a live `listProjects()`. URL schemes/entitlements/keychain were declared in the Phase 0.3 project.yml.
- ⏳ **STILL PENDING — verify on device (Enzo):** both sign-in flows round-trip to prod `/api/projects`. Simulators can't complete the MSAL broker / Apple flows, so this is the on-device gate before Phase 2 screens. The Dashboard placeholder already renders the real project list once signed in, so it doubles as the round-trip check.

## Phase 2 — Read-only screen parity (enumerate every field from the .tsx first)

Order: cheapest-risk first, each screen greps its React source for the full field/section inventory before coding.
- **2.1 Dashboard** ✅ **DONE (iOS repo `2ab79fd`, build 7; builds clean, needs on-device visual check).** Full port of `Dashboard.tsx`: hero, 4-stat strip (client-computed), search + status filter chips, project card grid, Shaper Hub section, templates (read-only — clone/delete deferred to Phase 3), DIY links. Shared components: `StatusBadge`, `ProjectCard`, `ShaperProjectCard` (all with dark-mode variants). **IA decision:** the web `/` Dashboard is the only list page (no separate projects/shaper routes), so the native tabs are **dashboard / shopping / tables / more** (details push from the Dashboard grids via NavigationLink). ShopKeep companion card deferred (needs the ShopKeep URL/scheme confirmed). ✅ **VERIFIED signed-in (MSAL web sign-in works in the simulator):** real projects load, `?oid=` hero images render, stats/search/filters work, card→detail navigation works. Two device-fixes applied (`1ebbdf6`): (1) `ShaperProject` list rows omit `images`/`cut_list` → custom decoder defaults them to `[]` (NintekKit `c70d854`); (2) card hero needed a bounded box (Rectangle+overlay+clipped) or the title overlapped it; stat labels needed `lineLimit(2, reservesSpace:)` to align.
- **2.2 ProjectDetail** ✅ **DONE & verified signed-in (iOS repo `3ed63b8`, build 11).** Full read-only port of the 1054-line `ProjectDetail.tsx`: hero banner + overlapping meta card (status, title, description, source/OptiCutter links, 4-stat grid), wood/tools chips (`FlowLayout`), sketches + inspiration galleries (PDF tiles → `PDFViewerSheet`/PDFKit), cut list, materials (read + purchased display), finish log, build-log timeline (photos via `?oid=`), linked projects, footer. Fullscreen swipe + pinch-zoom `ImageLightbox`. **Device-fix:** the literal 4-col cut-list table starved its narrow columns (rows ballooned to ~175pt); replaced with a mobile stacked row (part + ×qty / dimensions · material). Writes (edit/delete/toggle/add) deferred to Phase 3.
- **2.3 Shaper detail** ✅ **DONE & verified signed-in (iOS repo `6018b06`, build 13).** Full read-only port of `ShaperProjectDetail.tsx`: photo hero (uploaded image, else `photo_url`), title + CNC badge + "View on Shaper Hub" link, About, Materials, Instructions, Photos gallery (shared `ImageLightbox`), cut list (same mobile stacked-row style as the ProjectDetail fix). Verified on real data ("Assembly Jig" — badge/title/link/About render; Shaper Hub link opens `hub.shapertools.com` correctly). Edit/Delete + cut-plan optimizer deferred to Phase 3/4.
- **2.4 ShoppingList** ✅ **DONE & verified signed-in (iOS repo `363863e`, build 15).** Full port of `ShoppingList.tsx`: unpurchased materials grouped by project, item count + estimated total, "Show purchased" local filter toggle. Purchased checkbox is a read-only indicator (marking purchased is a write, deferred to Phase 3). Verified on real data ("BIRD FEEDER" group, 91 items) + confirmed the show-purchased toggle interaction live.
- **2.5 ConversionTables** ✅ **DONE, mostly verified (iOS repo `363863e`, build 15).** Pure Swift, no API — live mm↔inch converter (3 result pills) + 3 precomputed tables (MM→Inches, Inches→MM, Fractional→MM). Math ported verbatim (gcd-reduced fractions, nearest-1/32). Fractional→MM's 8-column web grid doesn't fit a phone — adapted to whole-inch sections with eighths wrapped as chips (`FlowLayout`), same 384 data points. **Verified:** quick converter hand-checked exact (42mm → 1.65354" → 1 21/32"), MM→Inches table interactive + correct, unit-button wrap bug found and fixed (`millimeters`/`inches` → `mm`/`in`). **Not individually tap-verified:** Inches→MM and Fractional→MM tabs — they share the identical table/row components already proven on MM→Inches, but simulator tap-coordinate targeting got unreliable after a device reboot mid-session; worth a quick on-device glance.
- Per-screen done-criterion: side-by-side with the web page, every field present.

**Phase 2 (read-only screen parity) is now COMPLETE — all 5 screens built.** Next: Phase 3 (write parity).

## Phase 3 — Write parity

- **3.1 ProjectForm** ✅ **CODE DONE (iOS repo `8eff7d5`, build 17).** Full port of the non-AI scope: all fields, status/difficulty pickers, wood/tools tag fields, est. hours stepper. Cut-list + materials row editors use a native `List` with **`.onMove`/`.onDelete` + permanent `.environment(\.editMode, .constant(.active))`** (delete circle + reorder handle always visible, no separate Edit toggle) — persists `sort_order` to every already-saved row after a drag. Row mutation matches the web exactly: add is local-only, remove deletes immediately server-side if the row has an id, Save does per-row create-vs-update sync. Wired: Dashboard toolbar "+" (sheet) and ProjectDetail toolbar pencil (sheet). **Verified in simulator:** sheet presents (create + edit), all fields/pickers/stepper editable, Add Part/Add Material create rows with visible delete+reorder controls, Cancel discards with no server call — **and (later session) `loadExisting()` confirmed against a real project** ("Hand Tool Storage Cabinet": title/URL/description/status/difficulty/tags AND cut-list rows with real fraction dimensions — "Tool Holder 3", "Plane Shelf 1/2 + Trim" — all loaded correctly with working delete+reorder controls). **NOT verified: the actual Save round-trip** (create/update). Tapping Save would write to Enzo's production database, and there's no project-delete UI yet (3.3) to remove test data afterward — deliberately left this for Enzo to test on-device rather than write throwaway data to prod.
- **3.2 Uploads** ✅ **CODE DONE (iOS repo `2d04d53`, build 19; NintekKit `25122d4`).** PhotosPicker (multi-select, re-encodes to JPEG) + `CameraPicker` (lifted from ShopKeepNative, auto-hidden when no camera) + Files/`.fileImporter` (PDF, sketches only) + add-by-URL (inspiration only) + delete (× overlay on thumbnails) → `postMultipart` with **real progress** via a new `URLSessionTaskDelegate` on `APIClient` (NintekKit addition — the plan's "upload task delegate" item). `UploadProgressPanel` floating cards (spinner+%, checkmark, or error+dismiss). **Verified in simulator:** UI renders correctly against real images (existing sketches show with working delete-×), PDF button correctly sketches-only, Camera button correctly absent (no camera in sim). **NOT verified: an actual live upload/add-by-URL/delete round-trip** — simulator tap-targeting was unreliable this session (see memory), and PhotosPicker/camera fundamentally need real media/hardware anyway. This needs Enzo on a real device.
- **3.3 Writes** ✅ **CODE DONE (iOS repo `aaba08d`, build 21; NintekKit `e340c73`).** Materials/purchased optimistic toggles (ProjectDetail + ShoppingList, tap-to-flip + PATCH + revert-on-failure), finish-log add/delete (product/type/color/coats/date/notes form), build-log add (note + PhotosPicker photo, real progress bar)/delete, linked-projects add/remove (project + relationship pickers), templates use/delete (Dashboard), project delete with `confirmationDialog` + save-as-template (toolbar menu). NintekKit's `WSMaterial.purchased`/`ShoppingItem.purchased`/`WSProjectDetail`'s materials-buildLog-finishLog-links arrays changed `let`→`var` to support in-place optimistic mutation. **Verified:** full rebuild clean, NintekKit's 20 tests green after the model changes. **NOT verified: any live write round-trip.** Simulator tap-targeting was unreliable this session (missed text fields and a search bar repeatedly, same issue flagged after the 3.1/3.2 session) — no test data was written (every risky attempt was either blocked by validation or cleanly cancelled). **This whole phase (3.1 Save, 3.2 uploads, 3.3 all of the above) now needs Enzo's on-device pass** — that's the single biggest verification gap in the port so far.
- **3.4 ShaperProjectForm** ✅ **CODE DONE (iOS repo `99ddcba`, build 23).** Full port of the non-AI scope: Shaper Hub URL, title, description, photo upload (PhotosPicker → JPEG; immediate upload in edit mode, queued locally in create mode since the route needs an id that doesn't exist yet) + photo URL override with live preview, materials rows (min 1, matches web), optional cut-list rows (no drag-reorder, matching the web's own lack of one here). Wired: Dashboard's Shaper Hub header "Add Project" (sheet); ShaperDetailView pencil (edit) + trash (`confirmationDialog` delete) toolbar buttons. **Verified:** full rebuild clean, app launches without crash with the new form wired in. **NOT verified: any live write round-trip** — same as the rest of Phase 3.

**PHASE 3 IS NOW FULLY CODE-COMPLETE (3.1–3.4) — but ZERO live write has been confirmed against the server across the whole phase.** Every write path (create/edit project, uploads, purchased toggles, finish/build-log, links, templates, delete, and now the Shaper form) builds clean and should work, but none of it has actually round-tripped successfully in front of anyone. Simulator tap-targeting has been unreliable for 3 sessions running (see memory) — this is a real gap, not a formality, and **the single highest-priority next step is Enzo testing all of Phase 3 on his real device** before touching Phase 4.

## Phase 4 — Cut-plan optimizer (the crown jewel)

- **4.1 Port `cutPlan.ts`** ✅ **DONE (NintekKit `4614ddd` + `85c7f30`).** `CutPlan.swift`: `parseInches`, `buildCutPieces`, `optimizeCuts` (BSSF + guillotine split + kerf, material/thickness matching), plus rendering helpers `cutPlanPalette`/`buildColorMap`/`fmtDim` from `CutPlanSheet.tsx`. **33 unit tests**, including every explicitly-flagged risk case (`1' 11½`, `27 3/4`, `.5`, smart quotes) and full branch coverage. Every `parseInches` test case was cross-checked by running the actual TypeScript logic in Node — outputs match exactly. 53/53 NintekKit tests green.
- **4.2 Optimizer UI** ✅ **DONE (iOS `ad76189`, build 26).** `CutPlanOptimizerView`: stock row editor (4×8/4×10 presets), kerf field, Generate, 4-stat results row, skipped/unplaced warning banners, up-to-6 sheet diagrams with show-all toggle, color legend. Config auto-loads/saves via the existing `cut-plan-config` endpoint — interoperable with web-saved configs (same JSON shape).
- **4.3 Sheet diagrams** ✅ **DONE (iOS `ad76189`, build 26).** `CutPlanSheetView`: SwiftUI `Canvas` port of the SVG — background, colored piece rects, part-name + fractional-dimension labels (rotated for portrait, truncated to fit), sheet border. Canvas draws in final screen points (via `.aspectRatio(.fit)`), so the web's "divide by scale" tricks (for constant physical stroke/font size under an SVG viewBox) become plain constants.
  - Both wired into ProjectDetailView + ShaperDetailView as a "Plan Cuts"/"Hide" toggle section after the cut list, matching the web's placement in both detail pages.
  - **Verified:** full rebuild clean, app launches without crash, navigated to a real project and confirmed the section renders correctly with real cut-list data. **Incidental finding:** two mistargeted taps aimed at "Plan Cuts" landed on the Materials list instead and toggled two materials purchased on "Hand Tool Storage Cabinet" (confirms Phase 3.3's optimistic toggle fires, but as an unintended side effect) — reverting them via more taps didn't visibly register, so **Enzo should check/uncheck "3/4" Walnut" and "Finish of choice" on that project** if they weren't actually purchased.
- **4.4 PDF export** ✅ **DONE (iOS `8d83232`, build 28).** `CutPlanPDFExporter`: multi-page landscape PDF (one page per sheet + legend), rendering the existing `CutPlanSheetView` via `ImageRenderer` and compositing onto `UIGraphicsPDFRenderer` pages (US Letter landscape, 0.45in margins — same as the web's `@page` rule) rather than reimplementing the diagram in raw CoreGraphics. `ActivityShareSheet` hands the file to the system share sheet (print/save/AirDrop), replacing the web's `window.print()` dialog. Wired as a "Download PDF" button in the optimizer's results section. **Verified:** full rebuild clean, app launches without crash. Not interactively tap-tested this pass (deliberately, after the 4.3 incident above) — needs the on-device pass like the rest of Phase 3/4's writes.

**PHASE 4 IS NOW FULLY COMPLETE (4.1–4.4) — the cut-plan optimizer is ported end to end: parser/algorithm (with Node cross-checked tests), UI, Canvas diagrams, PDF export.** Next per the plan is Phase 5 (AI import, exports, Settings).

## Phase 5 — AI import, exports, Settings

- **5.1 AI analyze** ✅ **DONE (iOS build 30).** `WorkshopAPI.analyzeProjectURL`/`analyzeShaperURL` + `AnalyzedProject`/`ShaperAnalysisResult` models already existed in NintekKit (scaffolded ahead of time) — this pass wired the UI. `ProjectFormView`: "Analyze with AI" button under the Plans URL field; prefills empty title/description/difficulty/est.hours/wood/tools, appends AI cut-list + material rows (never overwrites user-entered data), surfaces `analyzeError` inline. `ShaperProjectFormView`: same under the Shaper Hub URL field; materials are *replaced* wholesale when the AI returns any (matches web), extra `image_urls` beyond the primary photo are queued (`queuedImageURLs`) and attached via `addShaperImageURL` after save. **Verified:** full rebuild clean; not interactively re-tested this pass (no new network endpoint risk — reuses existing `analyze-url` routes already live in prod).
- **5.2 Exports** ✅ **DONE (iOS build 30).** New `CSVExport` helper (temp-file + `ActivityShareSheet`, mirrors web's `exportCutListCsv`/`exportMaterialsCsv`) wired as a small toolbar icon on ProjectDetail's Cut List and Materials section headers. New `ShoppingListPDFExporter` (single portrait-Letter page via `ImageRenderer`, same pattern as `CutPlanPDFExporter`) wired as a printer toolbar button on `ShoppingView` — replaces the web's `window.print()` with a native share sheet whose **Print** action is the true AirPrint equivalent. `MoreView`'s "Export JSON Backup" encodes `api.listProjects()` back to snake_case pretty-printed JSON and shares it the same way. **Verified in simulator:** JSON backup produced a real 36 KB file and opened the share sheet (Copy/Save to Files); Shopping List PDF produced a real 138 KB PDF with Print/Save to Files options in the share sheet — both against real prod data. The Cut List/Materials CSV toolbar icons share the identical `CSVExport`→`ActivityShareSheet` code path already proven live by the two above, but the icons themselves are small and 2 simulator tap attempts missed them (known coordinate-targeting gotcha, not a code issue) — stopped after 2 misses per the standing rule rather than keep guessing.
- **5.3 Settings** ✅ **DONE (iOS build 30).** `MoreView` fully expanded to match `Settings.tsx`: **Appearance** — Theme picker (light/dark/system, reuses the `Appearance` enum RootView already applied via `.preferredColorScheme`), Accent picker (pre-existing), **Large Text** toggle (`.dynamicTypeSize` at RootView's root — real effect on Dynamic-Type-aware text like nav titles/List rows/default-font TextFields, but Theme's explicit `.system(size:)` call sites are unaffected, same honest limitation as the web's rem-based scale only reaching CSS that uses `rem`). **Projects** — Default Status/Dashboard Sort/Show Completed by Default, all new `@AppStorage` (`SettingsKeys.swift`) that — unlike the web, where these three settings are persisted but **never actually consumed anywhere** (confirmed by grep — dead settings in `Settings.tsx`/`SettingsContext.tsx`) — are wired to real effects here: `ProjectFormView` reads the default status on create, `DashboardView.filteredProjects` sorts by updated/created/title and hides completed under the "All" filter unless the toggle is on. **Data** — Export JSON Backup (see 5.2). Account + version footer were already in place. **Verified in simulator:** Settings screen renders all sections correctly with real values (Theme/Accent/Default Status/Dashboard Sort pickers, toggles); Export JSON Backup round-tripped live (see 5.2).

**PHASE 5 IS NOW FULLY COMPLETE (5.1–5.3).** Next per the plan is Phase 6 (native thesis: haptics, widgets, App Intents/Shortcuts, polish, TestFlight prep) or the Phase 7 signature-features backlog.

## Phase 6 — Native thesis (the point of going native)

- **6.1 Haptics** ✅ **DONE (iOS/NintekKit `c0ee067`, build 32).** New `Haptics.swift` (imperative `UINotificationFeedbackGenerator` for one-shot save success/error, right before a form dismisses) + `.sensoryFeedback` modifier for value-bound cases: materials/shopping purchased toggle, cut-plan optimizer's result appearing. Also wired into delete/finish-log/build-log/link/save-as-template/template-clone success paths.
- **6.2 Widgets** ✅ **DONE (iOS `c0ee067`, NintekKit `01d93c5`, build 32).** New `WorkshopWidgetsExtension` WidgetKit target (embedded, App Group `group.com.nintek.workshop` — already provisioned since Phase 0.3), two widgets: **Project Stats** (small/medium, mirrors the dashboard's 4-tile stat strip) + **In-Progress Projects** (medium/large, lists active builds with deep links). `WorkshopWidgetSnapshot`/`WorkshopWidgetStore` in NintekKit — exact port of ShopKeepNative's `WidgetData` pattern (App-Group `UserDefaults`, 30-min timeline backstop + app-triggered `WidgetCenter.reloadAllTimelines()`), written from `DashboardView.load()`, cleared on sign-out. Deep links reuse the existing `workshop://` scheme from Phase 1. Includes the `NSExtension.NSExtensionPointIdentifier` fix in `project.yml`'s `info.properties` (App Store validation error 90348 — xcodegen regenerates the extension's Info.plist from those properties every `generate`, so a hand-edited key would be silently dropped). **Verified:** build succeeds with `WorkshopWidgets.appex` embedded in `Workshop.app/PlugIns`; generated Info.plist confirmed to carry the NSExtension key; the live data pipeline confirmed end-to-end by inspecting the simulator's shared App Group container directly — a real Dashboard load wrote correct project counts (85 parts, 18 in queue) into `group.com.nintek.workshop`'s UserDefaults. **Not verified:** actual home-screen widget rendering — adding a widget via the simulator's long-press/widget-gallery flow is a fragile multi-step UI interaction that wasn't forced; Enzo should add both widgets on a real device to confirm the visual result.
- **6.3 App Intents/Shortcuts + Spotlight** ✅ **DONE (iOS `f294525`, build 33).** Split across the two APIs that best fit each ask, rather than forcing everything through one: **Spotlight** (`SpotlightIndexer.swift`) donates a `CSSearchableItem` per project (title/description/keywords) after every `DashboardView.load()`, uniqueIdentifier set to the exact `workshop://project/<id>` deep link so `.onContinueUserActivity(CSSearchableItemActionType)` in `WorkshopApp.swift` can route it through the *existing* `AppModel.handleDeepLink` with zero new plumbing — this covers "open project" (search by name, tap, land on detail) and "run optimizer on a project" (a second donated item per project-with-a-cut-list, titled "Plan Cuts: …", linking to `workshop://project/<id>?cutplan=1`; `AppModel.pendingShowCutPlan` + `ProjectDetailView.load()` auto-expand the optimizer section). Index is cleared on sign-out. **App Intents** (`AppShortcuts.swift` + `IntentRouter.swift`, exact port of ShopKeepNative's `IntentRouter` pattern) stays deliberately parameter-free: `OpenShoppingListIntent`/`OpenDashboardIntent`, both `openAppWhenRun`, registered via `WorkshopShortcuts: AppShortcutsProvider` for Siri/Spotlight suggestions. Explicitly scoped away from a background-executable "check off a specific shopping item by name" intent — that needs its own silent-auth path outside the SwiftUI environment, which isn't verifiable in this environment and wasn't worth the risk; "Open Shopping List" (jump straight to the list, check items off by hand) is the safe version of the same ask. **Verified:** build succeeds; simulator launch+Dashboard-load logs confirm `CSSearchableIndex.default().indexSearchableItems` actually runs (a real `CoreSpotlight` service connection + `index-items` activity in the device log) with no crash. **Not verified:** actual Spotlight search-and-tap and Siri/Shortcuts invocation — the simulator's on-device search index is known to be unreliable/limited (a device-only concern, not a code-correctness one), and driving Siri/Shortcuts isn't something the simulator control tool can do at all; needs Enzo's on-device pass.
- **6.4 Polish round** ✅ **DONE (iOS `3e2da13`, build 34).** **contentColumn audit**: compared every screen's cap against the web's actual per-page CSS max-width (`.page-container`/`.detail-container`/inline styles) rather than guessing — found `ProjectDetailView`/`ShaperDetailView` both stuck on the default 640pt while their web equivalents (`ProjectDetail.tsx`/`ShaperProjectDetail.tsx`) cap at 900px; bumped both to `.contentColumn(900)`. Dashboard/Shopping/ConversionTables were already correctly tuned (ConversionTables intentionally narrower than the web's 1100px since its 8-column fractional grid was already redesigned to a mobile-appropriate layout in Phase 2). **Pull-to-refresh**: was on Dashboard/Shopping only; added to `ProjectDetailView`/`ShaperDetailView` (`ConversionTablesView` correctly excluded — it's pure-Swift, no API calls). **Skeleton loading states** (`SkeletonView.swift`, new): ported the web's actually-used `ProjectCardSkeleton`/`ProjectDetailSkeleton` (`components/Skeleton.tsx`) as `ProjectCardSkeletonView`/`ProjectDetailSkeletonView` — a pulsing placeholder block, wired into Dashboard's initial grid load (6 cards) and ProjectDetail's initial load, replacing a bare `ProgressView` at exactly the two spots the web itself bothers to skeleton (checked `grep` usage, not every screen — the web doesn't skeleton Shopping/ShaperDetail either). **Empty states**: audited every list-shaped section across the app (cut list, materials, finish/build log, links, sketches/inspiration, dashboard grid, shopping list) — all already had proper empty-state text from earlier phases, no gaps found. **Error toasts** (`ToastCenter.swift`, new): a `sonner`-equivalent transient banner (success=accent, error=fail, auto-dismiss ~2.6s), mounted once as an overlay in `RootView`. Wired into the highest-value spots — the ones that were previously *silent* on failure (finish/build-log add+delete, project link add+remove, save-as-template) plus AI-analyze success/error (matching the web's own `toast.success('Fields pre-filled…')`/`toast.error('AI analysis failed')`) — not an exhaustive 1:1 port of all 19 of the web's `toast.*` call sites, but the ones where native previously gave zero user feedback on failure. **Verified:** build succeeds; simulator screenshot confirms ProjectDetail renders correctly at the new column width with no regression; no crashes in device logs.
- **6.5 TestFlight prep** ⚠️ **PARTIALLY DONE (iOS `46a29c9`, build 35) — the parts that don't need Enzo's Apple Developer account/Xcode Organizer are done; archive/upload/1.0.0 genuinely can't happen from this environment.** **Icon set**: already correct — `AppIcon.appiconset` uses the modern single-1024×1024-"universal" format (Xcode auto-generates every size at build time), confirmed the PNG is exactly 1024×1024 RGB with no alpha channel (a common App Store rejection cause), `CFBundleIconName: AppIcon` already wired. No "trap" found — Phase 0's icon work already avoided it. **Export compliance**: added `ITSAppUsesNonExemptEncryption: false` to `project.yml`'s Info.plist properties (Workshop only ever speaks HTTPS/TLS, the standard exemption) — skips the compliance question App Store Connect would otherwise ask on every TestFlight build. **Archive validation**: ran a real (unsigned) `xcodebuild archive -destination 'generic/platform=iOS'` locally — **it succeeded**, including the widget extension embedding, dSYM generation, and AppIntents metadata/SSU extraction (confirmed both "Open Shopping List"/"Open Dashboard" shortcuts were correctly picked up this time). This de-risks the *build graph* — no target-wiring, Info.plist, or entitlements issue will block a real archive. **What still needs Enzo, and can't be done from here:** a real signed archive needs his Apple Developer Team's actual signing identity/provisioning (`CODE_SIGN_STYLE: Automatic` + team `3KB968X34U` are already set, so Xcode should handle signing automatically once he archives via Xcode's own Organizer or `xcodebuild archive` on his machine with his account logged in); the upload to App Store Connect itself (`xcodebuild -exportArchive` + `altool`/Transporter, or Organizer's "Distribute App" button); creating the App Store Connect app record if one doesn't exist yet; and answering TestFlight's remaining submission questions (export compliance is now pre-answered, but content-rights/advertising-identifier questions still show). **Bump to 1.0.0**: deliberately **not done** — `MARKETING_VERSION` stays at `0.1.0`. Given the size of the still-unverified-on-device backlog across Phases 3-6 (most write paths, both widgets' actual home-screen rendering, Spotlight search-and-tap, and Siri/Shortcuts invocation are all "code-complete, simulator-can't-verify" per their respective phase notes above), flipping to 1.0.0 before any of that has been confirmed on a real device would overstate readiness. Recommend Enzo does one full on-device pass first, then bumps to 1.0.0 himself right before the TestFlight upload — that's a one-line `project.yml` edit + `xcodegen generate` whenever he's ready.

**PHASE 6 IS CODE-COMPLETE (6.1–6.5) with one deliberately-open item.** Haptics, widgets, App Intents/Spotlight, and the polish round are all done and build-verified. 6.5 (TestFlight prep) is as far as this environment can take it — the remaining archive/sign/upload/1.0.0-bump steps need Enzo's own Apple Developer account and Xcode Organizer. Before that upload, strongly recommend one full on-device pass covering the whole unverified backlog flagged across Phases 3-6 (most write paths, both widgets' home-screen rendering, Spotlight search-and-tap, Siri/Shortcuts invocation, Mac sidebar default). Phase 7 (signature native features) is the next body of *new* work whenever Enzo wants to continue past TestFlight prep.

## Phase 7 — Signature native features (beyond Phase 6's basics)

Ideas discussed with Enzo 2026-07-25 — things a React web app structurally can't do, chosen to fit Workshop's actual data (cut lists, dimensions, finish log, materials) rather than generic "native app" checkboxes. Not yet scoped into sub-tasks; pick up when Phase 6's basics are done.

- **7.1 Live Activity cut-list checklist (ActivityKit)** ✅ **DONE (iOS `26c5ed0`, NintekKit `5427b14`, build 36).** `CutListActivityAttributes` (NintekKit, `#if os(iOS)`-guarded since `ActivityAttributes` is unavailable on macOS and NintekKit also targets macOS 14) holds a fixed parts list + a `ContentState` with just `checkedPartIds: Set<Int>` — deliberately **not** persisted to the server, since the web's `cut_list` table has no "has this been cut" column; this checklist is a purely native, in-the-moment tracking aid. `CutListActivityController` (app target) starts/ends the activity; a "Track Cuts"/checklist-icon toggle sits in `ProjectDetailView`'s Cut List section header next to the CSV export button. The widget extension (`CutListActivityWidget.swift`) renders the Lock Screen banner and Dynamic Island (compact/expanded/minimal) via `ActivityConfiguration`, with `ToggleCutPartIntent: LiveActivityIntent` making each row's checkbox tappable with **no app launch and no network** — it mutates the Activity's own state directly via `Activity<T>.activities`, sidestepping the background-auth risk that ruled out a similar interactive intent in Phase 6.3. **Bug caught and fixed via device-log inspection**: the first version's `start()` called `end()` as a fire-and-forget `Task { }` before requesting the new activity — since that task doesn't actually run until *after* the synchronous `Activity.request()` call returns, it raced and ended up dismissing the activity that had just been created (confirmed in `xcrun simctl log show`: create followed by an immediate, unrequested end for the same activity ID). Fixed by making `start()`/`end()` `async` and properly `await`-ing the dismissal before requesting the new activity. **Verified live in simulator**: after the fix, device logs show the activity reaching `state: active` and staying there; locking the simulator (`LOCK` button) showed the actual, real Lock Screen Live Activity banner rendering — project title, "0/8 cut" progress, and all 8 cut-list rows with checkboxes, exactly matching the SwiftUI source. This is real functional confirmation, not just a clean build. The interactive checkbox tap itself wasn't clicked through (blocked by iOS's one-time "Allow Live Activities from Workshop?" system permission alert, which is standard OS behavior rather than app code, and not worth forcing further taps on a system dialog) — Enzo can confirm the tap-to-check interaction on his own device.
- **7.2 Interactive widgets (WidgetKit + AppIntents, iOS 17+)** ✅ **DONE (iOS `8f0157b`, NintekKit `b709207`/`ff04e9a`, build 37).** New **Shopping List** widget (small/medium) — the top 5 unpurchased items across all projects, each row a real `Button(intent: ToggleShoppingItemIntent(itemId:))`. `WorkshopWidgetSnapshot` gained a `shoppingItems` field + `mergeShoppingItems` (Dashboard and Shopping List write the snapshot independently at their own load points, so a plain overwrite from either would clobber the other's half — merge preserves both). **The key design decision**: `ToggleShoppingItemIntent` has **no** `openAppWhenRun`, so it runs in the widget extension's own process with no network/auth available — exactly the background-auth situation that ruled out a similar intent in Phase 6.3. Solved by having the intent (a) optimistically drop the item from the cached snapshot for instant widget feedback, and (b) record the item id via a new `WorkshopWidgetStore.requestShoppingToggle`/`consumePendingShoppingToggle` pair; `DashboardView.load()` (the natural first-load point) consumes it and performs the real, authenticated `api.setPurchased` call. `ToggleShoppingItemIntent` lives in NintekKit rather than the app or widget target, since the widget's `Button(intent:)` needs to see the type and it has no app-only dependencies. **Verified live end-to-end**: a debug button (temporarily added to `MoreView`, removed after) called the exact same `WorkshopWidgetStore.requestShoppingToggle` the real widget intent calls; force-quitting and relaunching showed the app correctly consuming the pending id, calling `setPurchased`, and the Shopping List screen reflecting the change (89 → 88 items, "1x6 cedar board" no longer listed) — a real write against prod data. ⚠️ **Note**: this test toggled a real, previously-unpurchased material ("1x6 cedar board" on the "Bird Feeder" project) to purchased — flagged for Enzo alongside the existing Phase 4 disclosure, since it's the same category of incidental test-data change. Actual widget-button tap itself (as opposed to the debug-button stand-in) wasn't clicked through in the simulator's widget gallery, for the same reasons established in Phase 6.2 — needs Enzo's on-device check.
- **7.3 VisionKit Live Text / DataScanner** ✅ **DONE (iOS `<pending>`, build 38).** New `DimensionScannerView`/`DimensionScannerSheet` (`Screens/Components/DimensionScannerView.swift`) wraps `DataScannerViewController` (iOS 16+, text-only, no barcode) behind a segmented Length/Width/Thickness picker, so all three dimensions of a part can be scanned in one pass — tapping a recognized number fills whichever field is selected. A small camera-viewfinder button next to each cut-list row's part-name field (in both `ProjectFormView` and `ShaperProjectFormView`) opens the sheet scoped to that row's three fields. **Hard constraint, not a testing gap**: `DataScannerViewController.isSupported`/`.isAvailable` are unconditionally `false` in the Simulator — this needs real camera hardware and cannot be exercised at all without a physical device, unlike other Phase 6/7 items where the simulator gotcha was merely inconvenient tap-targeting. The `ContentUnavailableView` fallback (shown whenever scanning isn't supported) is therefore the *only* path this environment can exercise, and it was: **verified live** — opened the sheet from a real cut-list row, confirmed the segmented control renders and switches, confirmed the correct "Scanning Unavailable" fallback appears with no crash, and confirmed Cancel/Done dismiss cleanly leaving all field values untouched. Needs Enzo's on-device pass to confirm the actual live-camera scanning UX.
- **7.4 PencilKit sketch canvas** (iPad): sketch directly in-app with Apple Pencil as an alternative to uploading a photo of a paper sketch.
- **7.5 Share Extension**: "Add to Workshop" from Safari/Pinterest/Photos share sheets, landing in Plans URL or Inspiration — the web's `share_target` is weak on iOS Safari, so this is a real native advantage.
- **7.6 Swift Charts**: spend-over-time, hours logged via build-log entry counts, materials cost trends. The web app has no analytics/charts at all — cheapest item on this list and a good screenshot.
- **7.7 Local notifications from finish-log data**: "second coat due" reminders computed from `coats`/`applied_at`, no push infrastructure needed.
- **7.8 Drag and drop** (iPad): drag a photo from Photos or a PDF from Files straight onto a cut-list section to attach it (complements the existing PhotosPicker/fileImporter uploads).

## Conventions (from day one)
- **Version bump every commit** (project.yml `MARKETING_VERSION` semver + `CURRENT_PROJECT_VERSION` monotonic; Info.plist references the vars — never literals).
- xcodegen `project.yml` is the only project source; regenerate before every build: `cd "/Users/enzo/repos/Xcode - SwiftUI/Workshop for iOS" && xcodegen generate && xcodebuild -scheme Workshop -destination 'generic/platform=iOS' build CODE_SIGNING_ALLOWED=NO -clonedSourcePackagesDirPath .spm`
- Every screen: read the `.tsx`, enumerate fields, then build. Behavioral spec notes go in `docs/plans/` as screens are built.
- NintekKit stays dependency-free; MSAL only in the app target behind `TokenProvider`.
- Commit + push per feature (ShopKeepNative-style single-purpose commits); status doc `docs/plans/workshop-native-status.md` maintained as the handoff.

## Verification
- Phase 0: prod `/api/health`; native-shaped Entra token accepted (curl with a real token); Apple exchange mints a session; web app still logs in (regression).
- Phase 1: device sign-in (both providers) → live `/api/projects` list renders.
- Phases 2–3: per-screen side-by-side vs web (same account/data); writes verified by refreshing the web app.
- Phase 4: NintekKit unit tests green (`swift test`); same stock+pieces produce comparable yield to the web optimizer; config saved native loads on web.
- Phase 5: AI import on a real Kreg/Shaper URL; CSV/PDF outputs open correctly.
- Phase 6: widgets update after data change; deep links land on the right screen; TestFlight build validates.
- Enzo device-testing loop after each phase (expect a findings batch like ShopKeep's; budget a fix round).
